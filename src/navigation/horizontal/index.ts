import type { HorizontalNavItems } from '@layouts/types'

export default [
  {
    title: 'Evaluate MCQ',
    to: { name: 'evaluate' },
    icon: { icon: 'mdi-account-star' },
  },
] as HorizontalNavItems
